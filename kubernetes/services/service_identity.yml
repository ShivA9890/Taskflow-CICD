kind: Service
apiVersion: v1 
metadata:
  name: identity-service
  namespace: taskflow

spec:
  type: ClusterIP 
  ports:

    - name: identity-service
      targetPort: 8081
      port: 8081
      protocol: TCP
  
  selector:
    app: identity-service
